# Security Policy

墨水是纯前端、本地优先应用。作品、图片、设置和插件配置默认只保存在当前浏览器的 IndexedDB 中，不上传到墨水服务器。

## 支持范围

安全报告适用于：

- 实现或文档错误地上传、公开或泄露本地作品与图片。
- 导入文件绕过格式、大小、数量、MIME 或资源哈希校验。
- 导入或恢复失败后部分覆盖当前作品库。
- 导出 HTML 意外包含应用密钥、开发机路径或作品无关数据。
- Service Worker 缓存、GitHub Pages 配置或构建产物暴露不应发布的文件。
- 插件激活失败后留下 Hook、事件、贡献或配置残留。

## 本地数据

- 浏览器站点数据被清除时，本地作品可能一并删除；重要作品应定期导出整库备份。
- 多标签编辑使用写锁、revision 和广播保护，但整库恢复仍应在其他编辑标签关闭后执行。
- `dist/`、`player-dist/`、测试产物、日志、导出 ZIP/HTML 和浏览器数据不进入版本控制。
- 仓库不需要 JWT、邮件服务、数据库或部署密钥。

## 导入与导出

- 只导入来源可信的墨水 JSON、ZIP 和作品库备份。
- 导入必须先完整解码和校验，再提交 IndexedDB 事务。
- 独立 HTML 包含作品正文和引用资源；发布前应确认其中没有不希望公开的内容。
- 内建插件随应用静态打包。当前不加载远程插件代码。

## 报告方式

请通过私下渠道联系项目 owner，并提供问题描述、影响范围、复现步骤和涉及文件。不要在公开 issue 中粘贴真实作品、图片、浏览器数据或私有路径。
