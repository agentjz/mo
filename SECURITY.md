# Security Policy

墨水处理故事草稿、作者信息、上传图片、导出 HTML、用户数据目录和打包产物。这些数据默认视为敏感数据。

## 支持范围

安全报告适用于：

- 文档中错误描述数据上传、云同步或公开访问边界。
- 实现中泄露作者数据、草稿、图片、密钥或私有路径。
- 打包产物错误包含开发机临时文件、密钥、`node_modules` 缓存或用户数据。
- 后端执行不受控命令、加载不可信文件或暴露不应公开的目录。

## 数据处理原则

- 用户数据目录、构建产物、打包输出和运行日志不进入版本控制。
- `.env` 不进入版本控制。
- 样例素材必须是公开、非敏感、可分发的小样例。
- 导出 HTML 只包含作品运行所需数据，不包含作者私有凭据。

## 密钥和配置

- JWT secret、邮件服务凭据、私有部署地址不得进入版本控制。
- 示例配置使用 `production.env.txt` 或 `.env.example`，不得包含真实密钥。

## 报告方式

请通过私下渠道联系项目 owner，并提供：

- 问题描述。
- 影响范围。
- 复现步骤。
- 涉及文件或配置。

不要在公开 issue 中粘贴真实用户数据、密钥或私有路径。
