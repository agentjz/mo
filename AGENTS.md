# 墨水 Agent 工作规约

本文件是本仓库对所有 coding agent 的统一最高工作约束，不依赖具体工具或厂商。产品事实看 `spec.md`，当前任务执行状态看 `plan.md`。

## 基础约束

- 始终使用简体中文和项目 owner 交流。
- 阅读仓库文本时按 UTF-8；PowerShell 中文显示乱码时不得误判文件损坏。
- 在 PowerShell 中运行 Node/npm 命令时使用 `npm.cmd`、`npx.cmd`。
- 不回滚 owner 已有改动；脏工作区只处理当前任务相关文件。
- commit 或 push 前必须得到 owner 当前明确授权。
- 不使用阶段性版本命名组织产品事实、计划或回复。
- 禁止跨文件机械替换、批量正则替换和大小写不受控的命名迁移。
- 禁止兼容转发、旧别名、旧语义包装、历史命名适配层和过渡文件。
- 当前代码只服务当前生效实现。
- 通用 agent skill 只放在 `.agents/skills/`，不恢复依赖特定产品的 `.codex` 目录。

## 工作顺序

每次接手都先读事实，再做判断，最后行动。

1. Research：读取 `AGENTS.md`、`spec.md`、`plan.md`、相关源码、测试、配置、git 状态和可复现命令结果。
2. Plan：中大型开发、跨模块重构、架构硬化、恢复演练或生产级验收必须先维护根 `plan.md`。
3. Implement：按计划和当前架构边界完成实现，不偷偷偏离；新事实推翻计划时先改计划。
4. Verify：运行与风险匹配的局部测试，收尾前运行日常门禁和任务要求的独立验收。
5. Close：同步事实文档和 checklist，记录真实结果、未验证内容与剩余风险。

没有完成主链路调查，不动局部代码。不用口头说明代替计划更新，不交半成品。

## Plan 合同

`plan.md` 同时承担需求、当前事实、失败测试、目标、范围、设计、实施、验证和收口，必须按以下顺序组织：

1. 需求文档。
2. 当前事实。
3. 失败测试。
4. 目标。
5. 不做范围。
6. 设计。
7. 实施任务。
8. 验证计划。
9. 收口。

Checklist 必须随进度维护。事实推翻设计时先更新合同，再继续实现。

## 根文档边界

- `AGENTS.md`：所有 agent 的协作规则和仓库纪律。
- `spec.md`：当前产品事实、业务边界、技术边界和验收标准。
- `plan.md`：当前任务的单文件规格驱动执行合同。
- `plan.example.md`：后续任务计划模板。
- `README.md`：面向用户的产品介绍，只保留必要的本地使用入口，不写架构报告。
- `SECURITY.md`：本地数据、导入文件和导出产物规则。
- `CONTRIBUTING.md`：贡献流程和验证入口。
- `.gitignore`：不进入版本控制的文件边界。
- `.agents/skills/mo-development/SKILL.md`：墨水开发、架构、测试和交付规则。
- `.agents/skills/plan/SKILL.md`：`plan.md` 写作和执行规则。

除非 owner 明确要求，不创建额外规则体系或无关工程目录。

## Skills

- 修改编辑器、播放器导出、插件系统、游戏模组、保存、变量、图片热区、文档、测试或运行配置前，读取 `.agents/skills/mo-development/SKILL.md`。
- 中大型开发、跨模块重构和生产级验收前，读取 `.agents/skills/plan/SKILL.md` 并维护根 `plan.md`。

## 事实纪律

- 判断、计划、文档和回复只能基于当前仓库、命令输出、owner 输入或可复现结果。
- 不编造功能、数据、接口、页面行为、测试结果或业务规则。
- 不把未实现能力、旧实现或未来设想写成当前产品事实。
- 保留页面的可见文案和视觉效果不得无依据改写；静态页面文案统一放在 `src/content/`，页面组件不得内嵌大段正文或运行时加载 Markdown。

## 产品纪律

当前产品名是“墨水”。

墨水是纯前端、本地优先的桌面浏览器互动叙事编辑器：用节点流、图片热区、变量、Blockly 条件和插件体系创作互动叙事游戏，支持网页预览、作品导入导出、整库备份恢复和独立 HTML 播放器。

默认判断：

- 节点流编辑是主线；空间叙事和视觉小说式播放是核心体验。
- 浏览器 IndexedDB 是作品、图片、设置和插件配置的持久化 owner。
- 插件系统扩展编辑器和播放器能力，不把插件业务塞进 Engine。
- Engine 只管理节点跳转、选择、历史、存档和事件。
- RuntimePlugin 管理运行时变量；模板只读，Blockly 脚本通过注册函数修改变量。
- 保存必须防止闭包旧值、异步竞态、版本覆盖和多标签同时写入。
- 独立 HTML 必须脱离服务器运行。
- `start_index.py` 与 `npm.cmd run start` 是 owner 保留的本地构建和 `/mo/` 预览入口，不得删除或替换。

当前不做账号、邮件、云同步、在线作品库、公开发布、固定分享链接、Windows 便携包、远程插件安装、移动端编辑器、即时战斗、复杂数值养成和实时竞技。

## 架构纪律

依赖方向是 `domain -> application -> platform -> UI -> composition root`。下层不得反向依赖页面、组件、Context 或组合根。

- Engine 只处理节点流规则。
- PluginSystem 是插件状态、Hook、Event、Data Store、贡献和健康状态的唯一 owner。
- RuntimePlugin 是运行时变量唯一写入口。
- UI 不直接访问 IndexedDB、文件格式或插件内部实例。
- 导入先完整解码与校验，再在事务中提交；失败不得写入半成品。
- 保存写入带 revision、写锁和广播，旧版本不得覆盖新版本。
- 网页预览和独立 HTML 使用同一播放器业务源码。

单一职责看变化原因，不看行数。超过 300 行必须触发职责审查；状态、规则、渲染、存储、导出、测试工具和文档事实职责混杂时按变化原因拆分。

## 验证规则

依赖未安装时运行：

```powershell
npm.cmd install
```

日常门禁必须保持短，只包含类型、单元测试、生产构建和静态边界：

```powershell
npm.cmd run verify
```

浏览器和常规压力是独立命令，不得隐式加入日常门禁或 Pages 部署：

```powershell
npm.cmd run test:e2e
npm.cmd run test:stress
```

`npm.cmd run verify:full` 只用于人工全量验收。无法运行任何要求的验证时，必须写明具体命令、失败原因和剩余风险。

## 交付标准

把“顶尖标准”翻成可验收终局，不写“继续优化”。把 research、设计、实现、测试、文档和验证收成一次完整交付。不能闭环时说明客观阻塞、已完成事实和剩余风险。

最终回复短、准、硬：说明改了什么、验证了什么、是否还有风险；不粘贴大段日志、diff 或源码，除非 owner 明确要求。
