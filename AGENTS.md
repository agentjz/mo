# 墨水 Agent 工作规约

本文件是本仓库的最高工作约束。产品事实看 `spec.md`，当前任务执行状态看 `plan.md`。

## 基础约束

- 始终使用简体中文和项目 owner 交流。
- 阅读本仓库文本文件时按 UTF-8 读取；PowerShell 中文显示乱码时，不要误判文件损坏。
- 在 PowerShell 中运行 Node/npm 命令时使用 `npm.cmd`、`npx.cmd`，不要直接用 `npm`、`npx`。
- 不回滚 owner 已有改动，除非 owner 明确要求。
- 遇到脏工作区，只处理当前任务相关文件。
- 每次 commit 或 push 前必须先向 owner 确认；只有 owner 当前明确授权后才能执行。
- 不使用阶段性版本命名组织产品事实、计划或回复。
- 禁止跨文件机械替换、批量正则替换或大小写不受控的命名迁移。
- 禁止兼容转发、旧别名导出、旧语义包装、历史命名适配层或为了平滑过渡保留的中间文件。
- 当前代码只服务当前生效实现。

## 根文档边界

- `AGENTS.md`：agent 工作规则和仓库协作纪律。
- `spec.md`：当前产品事实、业务边界、技术边界、验收标准和开源协议。
- `plan.md`：当前任务的单文件规格驱动执行合同。
- `plan.example.md`：后续任务计划模板。
- `README.md`：面向用户和新读者的项目入口。
- `LICENSE`：MIT License 权威文本。
- `SECURITY.md`：安全报告、用户数据、密钥和打包产物处理规则。
- `CONTRIBUTING.md`：贡献流程和文档驱动规则。
- `.gitignore`：不进入版本控制的文件边界。
- `.codex/skills/mo-development/SKILL.md`：本项目开发、架构、测试、打包和交付规则。
- `.codex/skills/plan/SKILL.md`：`plan.md` 写作和执行规则。

除非 owner 明确要求，否则不创建额外 skill 体系或无关工程目录。

## Skills

项目级 skill 放在 `.codex/skills/`。

命中 skill 描述时，必须先读取对应 `SKILL.md`，再行动。

- 修改前端编辑器、后端 API、播放器导出、Windows 打包、插件系统、游戏模组、保存逻辑、变量系统、spec、README、AGENTS.md、tests 或运行配置时，先读 `.codex/skills/mo-development/SKILL.md`。
- 中大型开发、跨模块重构、生产级验收、恢复演练、架构硬化、评测闭环，或任何不能靠一次小补丁完成的任务，先读 `.codex/skills/plan/SKILL.md`，并维护根目录 `plan.md`。

## 事实纪律

- 事实高于一切。判断、计划、文档修改和回复必须基于当前仓库文件、命令输出、owner 明确输入或可复现结果。
- 不编造功能、数据、接口、页面行为、测试结果、打包能力或业务规则。
- 不把未实现能力写成当前产品事实。
- 文档只写当前事实和当前边界，不写假兼容、旧状态、旧数据或没有落点的未来设想。

## 产品纪律

当前产品名是“墨水”。

当前产品主线是：用节点流、图片热区、变量系统、Blockly 条件和插件体系创作互动叙事游戏，并支持在线编辑、独立 HTML 播放器和 Windows 便携版打包。

默认判断：

- 节点流编辑是主线。
- 空间叙事和视觉小说式播放是核心体验。
- 插件系统用于扩展编辑器和播放器能力，不把插件逻辑塞进核心引擎。
- Engine 层只管理节点跳转、选择、历史和存档，不写业务逻辑。
- RuntimePlugin 管理运行时变量；模板只读，Blockly 脚本通过注册函数修改变量。
- 保存逻辑必须保护数据完整性，避免闭包旧值、异步竞态和文件写入损坏。
- 独立 HTML 导出必须能脱离服务器运行。
- Windows 打包产物是可分发便携包，不把用户数据、临时目录和构建产物写入仓库。

当前不做：

- 即时战斗引擎。
- 复杂数值养成系统。
- 实时竞技。
- 云同步平台。
- 商城或素材市场。

## 文件职责

单一职责看变化原因，不看行数。

超过 300 行必须触发职责审查，但不是自动拆分理由。

职责混杂时必须按变化原因拆分：状态管理、规则计算、渲染展示、存储读写、打包流程、测试工具、文档事实不能硬塞在同一个文件里。

## 验证规则

- 代码任务收尾前必须运行根目录验证命令。
- 当前完整验证命令是 `npm.cmd run verify`。
- 如果依赖未安装，先运行 `npm.cmd run install:all`。
- 无法运行验证时必须说明具体命令、失败原因和剩余风险。

## 交付标准

接到明确问题后，把 research、设计、实现、测试、文档同步和验证收成一个完整交付。

不交半成品。

把“顶尖标准”翻成可验收的终局，不写成“继续优化”。

把任务定成生产级封顶验收，不写成后续优化或逐步改进。

不能一次闭环时，说明客观阻塞、已完成事实和剩余风险。

大改完成前运行项目完整验证命令。当前项目的完整验证命令必须在 `package.json` 中定义为：

```powershell
npm.cmd run verify
```

commit / push 只在项目所有者明确要求时执行。

## Caveman

短。准。硬。

少废话，不少判断。

少解释，不少证据。

少抽象，不少边界。

说不清，先别改。

## 收尾回复

- 最终回复保持简洁。
- 说明改了什么、验证了什么、是否还有风险。
- 不粘贴大段日志、diff 或源码，除非 owner 明确要求。
