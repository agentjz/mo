# 墨水体验等价架构重构与播放器模板平台 Plan

## 1. 需求文档

### 1.1 核心需求

以当前提交 `09d8d2f` 的界面、视觉、文案、交互路径和完整能力为不可破坏的产品基线，只重构内部模块，建立唯一故事核心、唯一运行核心和统一创作 API，使编辑器、节点图、场景、规则、网页试玩和独立播放器都成为同一份事实的投影；原样保留当前视觉小说与聊天播放器，在认真研究 `reference/` 中相关项目的职责边界、创作流程和模板机制后，增量加入结构与体验真正不同的播放器模板。架构升级不得表现为重新设计产品，不得让现有用户重新学习，不得删减现有能力、文案、图片或视觉细节。

### 1.2 使用者

- 使用节点流、图片热区、变量和 Blockly 创作空间探索、视觉小说、分支剧情和多结局故事的中文作者。
- 已熟悉当前作品库、编辑器、插件商店、视觉小说播放器和聊天播放器，希望升级后仍按原路径工作的现有用户。
- 需要制作可替换播放器模板、游戏模组、验证器、分析器和布局工具的扩展作者。
- 通过稳定、类型化命令读取和修改当前作品的本地创作工具。

### 1.3 完成后的体验

1. 首页、声明页、作品库、编辑器和插件商店的布局、层级、文案、配色、控件位置、操作结果与当前基线保持一致。
2. 用户仍从作品库创建或导入作品，在同一编辑器中编辑节点、选项、连接、文本、图片、人物、热区、变量和脚本，并按当前节奏自动保存与预览。
3. 当前视觉小说与聊天播放器的开始页、正文、选项、菜单、存档、地图、属性、背包、结局和返回行为保持一致。
4. 编辑器在现有播放器选择位置增量提供模板图库；模板选择不改变故事正文、连接、变量、规则、资源和存档语义。
5. 内置模板总数不少于十二套：两套为当前播放器的等价迁移，十套为结构、排版和交互组织明显不同的新模板，不得只换配色。
6. 网页试玩和独立 HTML 使用同一运行核心与同一模板实现；独立 HTML 在 `file://` 下完整运行且不请求墨水服务器。
7. 所有作品修改都通过统一创作 API 进入同一事务、revision 和撤销历史，页面、节点图、导入器和本地创作工具不能绕过它维护平行事实。

### 1.4 必须保留的能力

- 首页、声明页、现有对外文案、背景图、微信图片、微信二维码和全部现有视觉资产。
- 本地作品库、首次示例作品、创建、打开、删除、整库 ZIP 导出覆盖恢复和单作品独立 HTML 导出。
- 节点添加、编辑、拖动、连接、删除、标签、搜索、层次布局、辐射布局、验证、分析、统计、撤销和重做。
- 开始、普通、结局节点，选项、直接跳转、历史、回退、从头和从指定节点预览。
- Markdown、自定义文本效果、内嵌选项、人物对白和打字机速度的现有可见结果。
- 背景图、人物立绘、缩放、位置、对话框视觉参数、图片热区及其点击跳转。
- 数字、文本、真假变量，默认值、显示开关、显示顺序和运行时插值。
- 节点进入、节点离开、选项显示条件、选项执行脚本及 Blockly 编辑能力。
- 时间、生存模组及其变量、函数、积木、说明和现有运行结果。
- 视觉小说、聊天、菜单、新游戏、三个存档槽、读档、世界地图、属性、背包和结局体验。
- 插件搜索、分类、启停、依赖、冲突、健康状态、主题和配置持久化。
- 自动保存、revision、Web Locks 单写者、第二标签只读、BroadcastChannel 和关闭前脏状态提示。
- GitHub Pages `/mo/`、Hash 深链、PWA 离线应用壳、`start_index.py` 和 `npm.cmd run start`。

### 1.5 体验冻结规则

- `09d8d2f` 是现有页面与两套播放器的视觉和交互基线，不以新设计稿、通用后台布局或个人审美替换。
- 架构重构前必须建立同机、同浏览器、同数据、同视口的截图、DOM、可见文案、关键样式和交互基线；重构后逐项对比。
- 首页、声明页、作品库、编辑器默认态、各编辑面板、插件商店、两套播放器开始/游玩/菜单态均属于冻结范围。
- 现有控件的移动、改名、删除、合并、视觉降级或交互步骤增加都视为回归；确因模板入口新增而变化时，只允许局部增量，并必须保留原有路径和视觉语言。
- 不得通过更新基线截图、放宽截图阈值、删除断言或隐藏元素消除视觉失败。

### 1.6 工程与交付约束

- 纯前端、本地优先、桌面浏览器编辑；IndexedDB 继续持有作品、资源、设置和插件配置。
- 依赖方向保持 `domain -> application -> platform -> UI -> composition root`，下层不得反向依赖页面或浏览器实现。
- 断裂式更新当前内部结构和文件 schema，不迁移旧浏览器数据或旧作品文件，不保留旧别名、转发、包装、双写和过渡层。
- 通用能力进入核心；特殊玩法和表现通过类型明确、低耦合、可删除、可替换的插件贡献。
- `reference/` 是 owner 的本地研究仓库，不属于主仓库任务产物；不得删除、移动、修改、提交或清理其中内容。
- 未经 owner 当前对话明确授权，不 commit、不 push、不触发外部部署。
- 从 research、设计、实现、测试、文档同步、生产验收到最终收口一次性完成，不交阶段性替代品。

## 2. 当前事实

### 2.1 仓库基线

- 当前 `HEAD`、`master`、`origin/master` 均为 `09d8d2f`，提交说明为“重构为纯前端本地优先应用”。
- 根目录是唯一 React + Vite + TypeScript 应用；生产 base 为 `/mo/`，路由使用 HashRouter。
- 当前主仓库没有 `plan.md`；`reference/` 以未跟踪目录存在，包含九个独立浅克隆。
- `reference/` 当前包含 Chapbook、Harlowe 历史镜像、Ink、InkJS、Inky、Snowman、SugarCube、Twine Specs 和 TwineJS；只用于研究，不复制其代码、素材或文案。
- `start_index.py` 先构建再从 `/mo/` 提供本地生产预览；此入口和 `npm.cmd run start` 必须保留。

### 2.2 当前页面与视觉事实

- 路由为首页 `/`、声明 `/statement`、作品库 `/app`、编辑器 `/editor/:id`、播放器 `/play/:id` 和插件商店 `/plugins`。
- 首页与声明正文来自 `src/content/` 类型化模块；背景、微信图片和二维码位于 `src/assets/`。
- `Dashboard` 展示创建、整库 ZIP 导入导出、插件商店和作品卡片；作品卡片提供独立 HTML 与删除操作。
- `Editor` 使用 React Flow 主画布、左侧工具区、底部节点编辑区和连线编辑区；当前视觉由 `editor.css`、`editor-tabs.css` 及主题插件共同决定。
- 当前两套播放器是 `VisualNovelPlayer` 与 `ChatStylePlayer`；前者为全屏视觉小说布局，后者为居中手机聊天布局，各自有独立菜单组件和样式。
- 当前页面与播放器存在大量经过设计的专用 CSS；这些文件行数大不是删除或统一改色的依据，拆分必须保持最终计算样式和布局结果。

### 2.3 当前故事与运行事实

- `StoryDocument` 是作品语义唯一事实，`StoryEditorState` 单独保存 React Flow 坐标、视口和编辑器投影；Choice 的 `targetSceneId` 是唯一连接事实。
- `AuthoringSession` 是创作命令、revision、dry-run、原子批次、撤销和重做的唯一写入入口；React Flow 只通过适配器投影和提交命令。
- `PlayerKernel` 统一管理开始、选择、直接跳转、热区、历史、存档、变量、规则时间点、访问记录和结局，模板只消费快照并提交命令。
- `TemplateRegistry` 与 `TemplateCompiler` 统一网页试玩和独立 HTML；模板选择不需要向 PlayerKernel 或故事 schema 增加模板 ID 分支。

### 2.4 当前插件、规则与存储事实

- `PluginSystem` 已拥有注册、启停、Event、Data Store、类型化贡献、依赖、冲突、健康状态、失败清理和配置导入导出。
- 当前贡献包含运行时变量、规则包、验证、分析、布局、编辑器主题、播放器模板、内容渲染和编辑工具；Blockly、时间与生存通过明确贡献合同接入。
- `RuntimePlugin` 是运行时变量 owner，Blockly 脚本通过注册函数修改变量。
- IndexedDB `mo-workspace` 保存 stories、assets 和 settings；`WorkspaceRepository` 负责事务与 revision。
- `StorySaveCoordinator` 串行防抖保存；Web Locks、BroadcastChannel 和 revision 共同保护多标签写入。
- 导入器已对结构、资源数量、大小、MIME 和哈希做校验；整库恢复在事务中替换当前数据。

### 2.5 当前测试与构建事实

- `npm.cmd run verify` 包含类型检查、单元测试、生产构建和静态边界，不包含浏览器或压力测试。
- `npm.cmd run test:e2e` 覆盖 Pages 子路径、首次作品、编辑保存、多标签与 revision 冲突、插件配置、离线、整库 ZIP、十二模板和单作品独立 HTML。
- `npm.cmd run test:stress` 覆盖 100、500、1000 节点的搜索、选择、正文、拖动、连接、添加、保存、模板切换、试玩和刷新恢复。
- 冻结视觉基线、模板 SDK 合同与十二模板统一行为矩阵均为可长期执行测试；72 张三视口模板截图保存在任务产物目录。
- 超过 300 行实现已按变化原因审查并完成必要拆分；剩余文件由单一页面、插件 owner、仓储事务、适配器、主题声明或冻结示例数据负责，不按行数机械切分。

### 2.6 T001 实测基线

- 基线环境为 Windows、Node `22.19.0`、npm `10.9.3`、Playwright `1.62.1`，当前 `HEAD` 为 `09d8d2f1255003251c5543c948164a1f22c1f6ac`。
- `npm.cmd run verify` 用时 `6.003s`，10 个 Vitest 文件、114 个用例全部通过；类型、生产构建和静态边界通过。
- 基线构建的独立播放器为 `1,278.19kB`，gzip `352.10kB`；应用构建共 53 个文件、`8,111,342` bytes，PWA 预缓存 31 项、`3,301.07KiB`。
- 基线主要脚本块为入口 `182.85kB`、Dashboard `107.06kB`、Editor `222.19kB`、插件 `276.23kB`、PlayerRouter `35.37kB`、Blockly `847.70kB`；Blockly 已独立懒加载，但插件块仍包含编辑器与运行能力。
- `npm.cmd run test:e2e` 用时 `14.793s`，7 个 Chromium 用例全部通过，覆盖当前导入导出、两套独立播放器、恢复、Hash 子路径、保存冲突、插件配置和离线刷新。
- `npm.cmd run test:stress` 用时 `9.443s`，3 个 Chromium 用例全部通过；100/500/1000 节点加载为 `137/270/351ms`，刷新为 `119/129/182ms`，记录堆为 `27.6/91.7/86.4MB`。
- 冻结资产 SHA-256：`background.webp` 为 `F6CB55498731A97ACD4A16F6188960E2FA731E19BA59C1F96080C133156AE9A2`，`wechat-person.png` 为 `A9E6786F52267490472874F373D42D4F87644347CE9A4A808E5EEE4A09AC7E34`，`wechat-qr.png` 为 `8DCFCCA944693AE66FDCF7F4BF295A18118D2361A31FB2E41914F24233EF7110`。

### 2.7 T001 参考仓库研究结论

- 九个 `reference/` 仓库均为干净工作树，提交分别为 Chapbook `2385f90`、Harlowe `16e121d`、Ink `35c63e5`、InkJS `fcf4602`、Inky `3d46aa9`、Snowman `1c9d990`、SugarCube `ba17b12`、Twine Specs `74b3d89`、TwineJS `e36c600`。
- 可借鉴原则：编辑器只维护创作事实与投影；编译器生成独立、版本化运行文档；运行核心拥有可序列化状态、选择和历史；展示层消费快照；扩展通过带版本和 owner 的显式注册表进入，编辑器扩展可关闭且不得成为作品运行的前提。
- 可借鉴原则：独立产物只装入当前故事、当前运行核心和当前展示实现；模板或格式的 manifest 明确身份、版本、能力与占位符；状态保存不序列化 DOM、编辑器坐标或扩展实例。
- 不复制内容：不复制任何参考仓库的代码、宏语法、CSS、模板 HTML、视觉资产、文案、示例故事或许可证文本；墨水使用自己的 StoryDocument、Authoring API、PlayerKernel 和模板视觉。

### 2.8 T002 体验冻结基线

- 基线捕获脚本为 `tests/visual/experience-baseline.spec.ts`，固定作品、时间、ID、视口并关闭非确定动画；原始产物位于忽略目录 `artifacts/experience-baseline/09d8d2f/`。
- 40 个状态截图全部非空，覆盖首页、二维码弹层、声明、作品库、插件商店、编辑器默认/节点/连线面板，以及两套播放器的开始、游玩、菜单、结局；冻结页面使用 `1440x900`、`1920x1080`，播放器增加 `390x844`。
- `contract.json` 记录每个状态的 Hash 路由、可见文案、可操作控件顺序、landmark 数量、根元素计算样式与边界框，并记录创建、打开、编辑、开始、菜单和分支选择的操作步数。
- 基线捕获用时 `8.813s`，40 个状态全部完成，浏览器 console error 为零；人工抽查首页背景、编辑器双侧布局、视觉小说桌面态和聊天手机菜单均与 `09d8d2f` 实际结果一致。

### 2.9 T003 失败测试与静态边界证据

- `tests/fixtures/acceptance-matrix.json` 与 `AcceptanceMatrix.test.ts` 将 F001–F018 映射到长期单元、静态、浏览器、视觉、离线、恢复和压力验收入口，18 项均有明确执行载体。
- `ExperienceBaselineContract.test.ts` 的 4 项体验保护合同全部通过，确认冻结文案、资产引用、样式入口与基线产物未因测试建设发生漂移。
- StoryDocument、AuthoringSession、PlayerKernel、RuleEngine、TemplateRegistry、TemplateCatalog、TemplateCompiler 和 TemplatePackageService 目标测试在旧实现上因目标模块不存在按预期失败，证明唯一故事核心、统一创作事务、唯一运行核心和模板平台尚未实现。
- `scripts/check-architecture.mjs` 在旧实现上按预期报告 21 个缺口：8 个目标模块缺失、共享领域类型依赖 React Flow、3 处 UI 越层、7 个并行核心或固定播放器入口，以及 Domain 中具体播放器标识。
- 创作端口、十二模板、按需加载和可访问性浏览器测试骨架已建立；这些用例保持真实失败或待目标入口实现，不通过跳过、弱化断言或修改体验基线收口。

### 2.10 T011–T018 执行事实

- `TemplateCompiler`、网页试玩与独立 HTML 共用 `createPlayerStateMachine`、`createRuleRuntime`、内容渲染和模板注册合同；独立产物只内联作品实际引用的规则函数，十二套 `file://` 产物 HTTP(S) 请求和 console error 均为零。
- 模板 01/02 保留 09d8d2f 的专用 React 页面、开始页、菜单和样式；冻结矩阵中未批准状态逐像素通过，Dashboard 的整库按钮与编辑器模板入口仅作为 owner 已批准的局部增量处理，基线文件未更新。
- 模板 03–12 具有十二种不同结构指纹、设置、场景变体、人物与热区降级、开始页和完整菜单；网页试玩和独立 HTML 各生成 36 张三视口截图，共 72 张，位于 `artifacts/experience-current/09d8d2f/templates/`。
- 模板矩阵覆盖 `1440x900`、`1920x1080`、`390x844`，自动检查非空、水平溢出、按钮裁切、指定节点、场景变体、图片、多人物、键盘热区、HUD、菜单和三个存档槽；人工抽查章节书页、视觉小说、聊天手机和桌面卷宗构图。
- 本地模板 ZIP 已覆盖安装、刷新恢复、卸载、哈希/脚本/路径/远程内容拒绝与持久化失败回滚；浏览器失败回路确认 catalog 和当前作品不变。BrowserAuthoringPort 已覆盖查询、schema、审阅、全部/部分接受、拒绝、过期 revision、无效引用、撤销与重做。

### 2.11 T019 执行事实

- `tests/fixtures/canonicalStory.ts` 使用自有内容覆盖分支、循环、两个结局、Markdown、内嵌选择、图片、三个人物降级、热区、数字/文本/真假变量、四类规则时间点、时间/生存函数和 Blockly 规则投影；模板浏览器矩阵使用同一夹具验证模板切换、菜单与三个存档槽。
- 两份首次示例对 `09d8d2f` 的标题、作者、说明、入口、播放器模式、全部节点 ID/类型/正文、全部选项 ID/文案/目标、全部节点坐标和变量做完整归一化比较；生存示例 SHA-256 为 `dc44bbf592ffcd87fe0b8815758019968cf60cb4888425ffba6b3a6e3379db31`，疑案示例为 `3e15f7b6e4983db8a6b4d438fab551707f57fe6bbf20a15319e3ceb53fe110d1`，均与基线相等并已固化为单元测试。
- T019 收口时 `npm.cmd run type-check` 通过；七个定向测试文件共 20 项通过，补充的 StoryDocument、WorkspaceRepository、PlayerKernel、RuleEngine 四文件共 13 项通过。

### 2.12 T020 执行事实

- 常规压力已扩展为 100/500/1000 节点的搜索、选择、正文编辑、拖动、连线、添加与保存、模板切换、试玩和刷新恢复；最终全量验收加载为 `114/217/330ms`，刷新为 `98/323/452ms`，记录堆约为 `37.3/81.4/53.5MiB`，可见节点为 `90/450/432`，单项操作最大 `1263ms`，均在预算内。
- 压力用例使用 `PerformanceObserver` 记录长任务；最终全量验收 100/500 节点为零，1000 节点为 20 个且最大 `87ms`。三档均验证保存后的正文、连线目标、模板和新增节点可在刷新后恢复。
- 浏览器可访问性覆盖模板图库首次聚焦、Tab 约束、Escape 关闭与焦点返回，背景图和三处立绘空态支持 Enter/Space 上传；长标题与长正文通过 `1440x900`、`1920x1080`、`390x844`，并通过 125%/150% 等效逻辑视口、4.5:1 文本对比度和减少动画断言。
- 超过 300 行的实现已按变化原因审查：模板包生命周期从 EditorSidebar 分离，NodeVisualPanel 按对话框、背景、热区和立绘工作流拆分，Editor 的锁/revision/保存/创作端口会话抽入 `useEditorSession`。剩余超限文件分别由冻结示例数据、主题声明、单一节点内容工作流、单一播放器、变量管理、插件 owner、适配器或仓储事务单独拥有，不做机械切分。
- 重构后生产构建入口 `183.73kB`（gzip `61.15kB`）、Editor `238.88kB`、Blockly `851.85kB`，均在 T001 预算内；保存/创作 4 文件 12 项单测、11 条相关浏览器用例、全链压力和冻结视觉基线均通过。

### 2.13 T021 执行事实

- README、spec、SECURITY、CONTRIBUTING 与单元测试说明已同步为当前 StoryDocument、AuthoringSession、PlayerKernel、十二模板、整库 ZIP 和单作品独立 HTML 事实；五份用户文档不再声明单作品 JSON/ZIP 入口。
- 整库恢复长期测试覆盖完整往返、坏 ZIP、错误 manifest、损坏作品记录、缺图、错误 MIME、哈希不符、重复资源、容量不足和事务提交失败，并逐次比较失败前后作品、资源和设置不变；浏览器用例确认坏 ZIP 入口恢复可操作且作品卡片不变。
- 已删除旧 CoreEngine、PlayerCore、PlayerRouter、旧 Story schema、固定播放器构建、旧测试、无消费者兼容入口和旧忽略路径；目标架构探针确认核心与导出路径不存在模板 ID 特判，旧路径仅保留在静态禁止存在清单中。
- T021 收口时类型检查、静态边界、目标架构检查、7 文件 35 项恢复/插件/保存/模板/导出定向单测和 3 项导出浏览器用例均通过；生产构建通过，入口 `183.73kB`（gzip `61.15kB`）。

### 2.14 T022 执行事实

- 最终规定命令按 `verify -> test:e2e -> test:stress -> verify:full -> start` 顺序真实执行；首次 `verify` 发现 ValidatorPlugin 的旧单结局夹具前提，修正为清除规范作品全部结局后定向通过并从头重跑，最终没有跳过或重试掩盖失败。
- `verify` 最终 19 文件 65 项单测、类型、生产构建和静态边界通过；`test:e2e` 16 项通过；`test:stress` 3 项通过；`verify:full` 的 65 项单测与 19 项浏览器/压力用例通过；冻结视觉基线另行重跑 1 项通过。
- `npm.cmd run start` 完成生产构建并在 `http://127.0.0.1:4567/mo/` 提供服务；独立浏览器探针确认 Hash 深链、Service Worker 控制、离线刷新、两份首次作品、模板图库单 iframe 懒加载、真实 `srcdoc` 和零 console error，验收后停止本次 Node/Python 进程并确认端口无监听。
- 模板产物目录最终包含网页 36 张、独立 HTML 36 张，共 72 张；九个 `reference/` 仓库均为干净工作树且提交未变，主仓库 HEAD、master 与 origin/master 仍为 `09d8d2f`，未 commit、push 或部署。

### 2.15 T023 接手事实

- `cac7ca1` 已推送到 `origin/master`，工作树干净；`reference/` 已由根 `.gitignore` 忽略，九个嵌套仓库仍干净。
- `npm.cmd audit --omit=dev --json` 稳定报告 2 个 high，实际是 `react-router-dom@7.18.2 -> react-router@7.18.2` 同一公告 `GHSA-qwww-vcr4-c8h2` 在直接与传递依赖上的两次计数；受影响范围为 `react-router >=7.12.0 <8.3.0`。
- npm 当前 latest 仍为 `7.18.2`；初次审计建议 `react-router-dom@7.11.0`，但重新审计证明 7.11.0 仍命中多条 high，不能作为修复版本。审计随后明确给出 `react-router-dom@6.30.4`，它同步修复 `react-router` 与 `@remix-run/router`，要求 Node 14+、React/React DOM 16.8+，与当前环境兼容。
- 当前代码只使用 HashRouter、Routes、Route、Navigate、Outlet、useNavigate、useParams 和 useSearchParams，不使用 RSC、Server Action 或服务端路由处理器。
- 6.30.4 安装树为 `react-router-dom@6.30.4 -> react-router@6.30.4 -> @remix-run/router@1.23.3`；生产与完整 `npm audit --audit-level=high` 均退出 0，high/critical 为 0，完整 audit 仍有 2 个 moderate。npm 提供的回退修复是 7.18.2，会重新引入原 high，不能采用。
- 依赖变更后独立 `test:stress` 三档通过，1000 节点刷新 `438ms`；定向复现为 `285ms`。现有 `verify:full` 在 E2E 后运行 stress 时，即使拆成独立 Playwright 进程，1000 节点刷新仍为 `595/610/605ms`；重负载模板/file:// E2E 结束后的宿主资源压力会污染随后的性能测量。

## 3. 失败测试

以下项目必须先形成测试、静态探针或可复现验收。对尚未实现的目标能力，测试应先证明旧实现失败；对现有体验，测试应先在 `09d8d2f` 上通过并冻结为保护基线。

### F001 现有视觉与交互漂移

- 为冻结状态建立 1440×900、1920×1080 桌面截图，以及聊天播放器常见手机视口截图。
- 记录可见文案、主要 landmark、关键控件顺序、计算样式、边界框、焦点顺序和操作步数。
- 任一现有页面出现布局替换、控件丢失、单一强调色覆盖、文本溢出、遮挡或步骤增加时失败。

### F002 品牌文案和视觉资产丢失

- 静态边界断言首页文案、声明内容、背景图、微信图片和二维码仍由原入口消费并进入生产构建。
- 对文件删除、正文内嵌回页面、运行时加载 Markdown 或资源引用失效直接失败。

### F003 Domain 依赖编辑器或浏览器

- `src/domain/`、运行核心与模板合同不得 import React、React Flow、页面、组件、Context、IndexedDB 或浏览器存储。
- 故事规则、命令事务和播放器状态必须能在纯 Node 单元测试中运行。

### F004 选择与连线存在双重事实

- 覆盖创建选项、连接目标、改目标、删选项、删场景、撤销、重做和批量命令。
- 每个选择只能有一个目标事实；结构视图连线只允许由该事实投影，不能留下幽灵边或悬空双写。

### F005 修改可绕过统一创作 API

- UI、节点图、场景面板、变量、规则、导入器和本地创作端口的写入必须经过同一命令事务。
- 无效命令、批量中任一失败、过期 revision 和 dry-run 不得改变作品、编辑状态、资源引用、撤销历史或保存 revision。

### F006 保存闭包旧值与多标签覆盖

- 覆盖连续快速输入、切换节点、返回作品库、刷新、写入延迟、旧 revision、第二标签和广播冲突。
- 最后一次可见编辑必须持久化，旧值和只读标签不得覆盖新 revision。

### F007 规则被 Blockly 私有拥有

- 同一条件和动作在自然结构、Blockly 投影、命令 API 与运行核心中的语义必须一致。
- 关闭 Blockly 或卸载可选规则包不得丢失通用规则事实；缺失特殊贡献必须给出明确验证问题。

### F008 两套现有播放器回归

- 对视觉小说与聊天播放器分别验证开始、正文、打字、内嵌选择、普通选择、图片、人物、热区、变量、菜单、三个存档槽、地图、属性、背包、结局、返回和重开。
- DOM 结构、视觉截图、可见文案和交互结果必须与基线等价。

### F009 新模板需要核心特判

- 注册测试模板后，无需修改 PlayerRouter、运行核心、导出器或故事 schema 的模板 ID 分支即可预览和导出。
- 删除测试模板只产生明确的模板缺失错误，不破坏故事语义、其他模板或存档。

### F010 模板能改写核心状态

- 模板只能读取 PlayerSnapshot 并提交 PlayerCommand；不能 import 仓储、编辑器、引擎内部实例或直接写变量。
- 切换模板前后，同一规范作品的可达场景、变量结果、历史和存档格式完全一致。

### F011 模板只是换色或能力静默丢失

- 十二套模板必须有结构指纹：shell landmark、内容布局、菜单组织和场景呈现至少存在明确结构差异。
- manifest 必须声明能力和降级方式；不支持的视觉能力必须可见降级或阻止选择，不能静默丢内容。

### F012 模板包与插件包安装留下半成品

- 覆盖 manifest 缺项、重复 ID、版本不符、依赖缺失、冲突、资源越界、大小超限、哈希错误、脚本异常和设置错误。
- 安装或启用失败必须清理 owner 贡献、恢复原配置并且不留下 IndexedDB 半记录。

### F013 导入和恢复非原子

- 覆盖损坏 ZIP、错误 manifest、损坏作品记录、缺图、错误 MIME、哈希不符、重复资源、容量超限和事务中断。
- 完整解码与校验前不得写入；失败后作品、资源、设置和 revision 与操作前一致。

### F014 网页试玩与独立导出分叉

- 同一规范作品、同一模板、同一命令序列在网页试玩和独立 HTML 中生成一致 PlayerSnapshot 与可见结果。
- 独立 HTML 的 `file://` 验收必须捕获并断言 HTTP(S) 请求为零。

### F015 插件生命周期泄漏

- 启停、卸载、激活失败和设置回滚后，不得残留 Event、Data、贡献、监听器或模板缓存。
- 依赖和冲突处理必须确定、可回滚，并呈现真实健康状态。

### F016 性能与加载回归

- 首页首屏不得加载 React Flow、Blockly 或播放器模板运行代码。
- React Flow 仅在编辑器需要时加载，Blockly 仅在打开脚本编辑时加载，模板代码仅在预览、选择或导出时加载。
- 100、500、1000 节点在 T001 实测机器上按基线设预算，不得通过放宽超时掩盖回归。

### F017 可访问性和布局破坏

- 所有现有可操作控件保留键盘访问、可见焦点、可读名称和合理对比度。
- 1440×900、1920×1080、125%/150% 缩放和长标题、长选项、长正文下不得出现非预期页面滚动、遮挡和不可达按钮。

### F018 旧实现和转发层残留

- 最终 `rg` 与静态边界确认不存在并行 Story/Engine/Player 事实、固定模板 ID 分支、旧 schema 解析、旧入口转发、兼容包装和无消费者文件。
- 删除旧实现前必须先证明所有现有能力已由当前实现和长期测试承接。

### F019 生产依赖审计高危项

- `npm.cmd audit --omit=dev --audit-level=high` 对任何 high 或 critical 返回失败；修复前当前锁文件稳定复现 2 个 high。
- 修复不得依赖 `--force`、忽略公告、审计豁免或只改 manifest 不更新锁文件；安装树必须同时解析到安全的 react-router-dom 与 react-router。

## 4. 目标

- **G001 体验等价**：冻结范围内的页面、文案、资产、控件、操作路径和两套播放器通过结构、视觉与浏览器等价验收。
- **G002 唯一故事核心**：当前 StoryDocument 是正文、选择目标、媒体、变量、规则、表现配置和扩展数据的唯一事实；React Flow 仅是投影。
- **G003 统一创作 API**：所有修改通过版本化命令、dry-run、原子事务、expected revision、结构化 diff、撤销和重做进入。
- **G004 唯一运行核心**：开始、跳转、选择、热区、历史、变量、规则时间点、存档、访问记录和结局由同一 PlayerKernel 管理。
- **G005 类型化扩展平台**：通用核心不含具体模板和特殊玩法分支；模板、规则包、验证、分析、布局、主题和编辑工具均由明确贡献合同接入。
- **G006 十二套完整模板**：保留两套当前播放器并新增十套结构不同模板，全部通过规范作品、设置、降级、存档、离线和独立导出矩阵。
- **G007 本地数据可靠**：保存、锁、广播、资源回收、导入、导出和整库恢复在竞态与失败场景下保持原子和可恢复。
- **G008 性能不退化**：日常门禁保持短，首屏不加载重型编辑或模板代码，大图和构建指标不劣于 T001 基线预算。
- **G009 文档与代码一致**：`spec.md` 只写最终已实现事实，README 保持用户文档定位，安全与贡献文档准确描述当前边界。
- **G010 一次性收口**：T001–T023、F001–F019 和其余目标全部有真实证据，未完成项归零后才结束。
- **G011 生产依赖审计阻断项归零**：生产与完整依赖审计均为 0 个 high/critical，剩余 moderate 公告有明确范围与攻击面记录；路由、构建、浏览器、压力、视觉和本地生产入口保持通过。

## 5. 不做范围

- 不重新设计首页、作品库、编辑器、插件商店或现有播放器，不建立新的六视图信息架构。
- 不以统一设计系统为名覆盖当前专用样式，不把现有丰富配色改成单一强调色，不做无依据文案改写。
- 不删除或改造 `reference/`，不把参考仓库代码、素材、文案或许可证内容复制进产品。
- 不增加后端、账号、邮箱、登录、云同步、在线作品库、公开发布、固定分享链接、商城或远程扩展市场。
- 不做 Windows 打包、移动端编辑器、即时战斗、复杂数值养成或实时竞技。
- 不迁移重构前的浏览器数据或作品文件，不保留旧 schema、旧别名、转发层和双写层。
- 不为未来设想增加没有当前消费者的抽象，不把所有内部函数改造成 Hook 或插件点。
- 不通过跳过测试、放宽超时、删除断言、更新视觉基线或伪造人工结果完成收口。
- 不借依赖安全修复升级 React、React DOM、Marked、jsdom 或其他与公告无关的包。
- 未获当前授权不 commit、不 push、不运行外部部署。

### 1.7 Owner 补充决策

- 不再提供单作品 JSON 导入导出或单作品 ZIP 导入导出；删除对应按钮、格式、解析器和测试，不保留旧入口或兼容读取。
- 工作区数据交换只保留一个整库 ZIP：导出必须包含全部 StoryDocument、StoryEditorState、revision、图片资源、设置和插件配置；导入必须先完整解码与校验，再在一个 IndexedDB 事务中覆盖当前工作区，失败时当前内容保持不变。
- 单作品独立 HTML 继续作为发布产物保留，不属于工作区备份格式。

### 1.8 Owner 依赖安全决策

- 消除 `npm audit` 报告的生产依赖高危项，不以当前纯前端应用没有 RSC 攻击面为理由保留红色审计结果。
- 使用明确、可复现的安全版本，不运行 `npm audit fix --force`，不顺带升级 React、Marked、jsdom 或其他无关依赖。
- 依赖调整必须通过审计、路由、日常、浏览器、压力、视觉和本地生产入口验收，保持 `/mo/`、Hash 深链、PWA 和现有视觉交互不变。

## 6. 设计

### 6.1 体验基线与增量原则

- 在任何 UI 文件修改前，以固定种子数据记录当前页面状态矩阵、截图、DOM landmark、文案、关键计算样式和边界框。
- 将可跨平台稳定的文案、控件顺序、路由、焦点、CSS token、资源哈希和归一化布局数据固化为测试夹具；原始截图保存到忽略目录用于同机人工对比。
- 架构迁移采用“先加内核与适配器、再逐入口接线、每接一处立即等价验收”的顺序，禁止先删整套 UI 再重写。
- 模板选择器只在当前播放器选择入口做局部增强，复用当前控件、间距、色彩和层级，不重排作品库或编辑器主结构。

### 6.2 唯一故事核心

- `StoryDocument` 只保存作品语义：meta、scenes、content、choices、media、variables、rules、presentation 和 extensionData。
- `StoryEditorState` 保存节点坐标、画布视口、面板选择和其他仅编辑器需要的投影状态；不得进入播放器语义。
- Choice 的 `targetSceneId` 是唯一连接事实；React Flow adapter 从 Scene 与 Choice 生成 Node/Edge，并把连接操作转换为创作命令。
- Domain 使用 Zod 在文件边界解析，内部保持不可变值语义；不 import React Flow、React 或浏览器接口。
- 删除场景、修改目标和资源引用必须由领域规则给出明确级联结果，不能由页面分别维护。

### 6.3 统一创作 API

- `AuthoringQuery` 提供按任务裁剪的只读快照与 schema，不暴露可变对象、IndexedDB 或插件内部实例。
- `AuthoringCommand` 使用显式版本和判别联合，覆盖作品信息、场景、内容、选项、连接、媒体、热区、变量、规则、模板和编辑投影。
- `AuthoringSession` 负责 schema 校验、引用校验、能力校验、dry-run、原子批量应用、expected revision、结构化 diff、撤销和重做。
- 一个批次只有全量成功才产生一个新 revision 和一个撤销记录；部分接受由调用方先筛选命令，再作为新的原子批次提交。
- UI handler 只组装命令和呈现结果；保存协调器订阅已提交快照，不持有另一份可写 Story。
- 浏览器创作端口仅暴露 `query`、`schema` 和 `submit`，提交结果进入当前编辑器的明确审阅与接受流程，不能直接静默落盘。

### 6.4 唯一运行核心

- `PlayerKernel` 仅处理开始、选择、直接跳转、热区、历史、返回、规则时间点、变量、存档、访问记录和结局。
- 运行状态通过不可变 `PlayerSnapshot` 投影，外部只能提交类型化 `PlayerCommand`；模板不能获取 Engine、RuntimePlugin 或仓储实例。
- `PlayerSaveAdapter` 抽象三个存档槽与自动存档；浏览器实现使用 localStorage，测试实现使用内存。
- 菜单、地图、属性和背包是从同一快照派生的只读投影；两套现有菜单可以保留各自 DOM 和样式，但不再复制业务规则。
- 内容渲染统一处理 Markdown、自定义文本效果、内嵌选项、人物对白、打字速度和变量插值；模板只决定放置方式。

### 6.5 规则与变量

- `RuleDocument` 保存通用条件、组合条件和动作，不以 Blockly XML 或生成代码作为唯一事实。
- `RuleEngine` 在场景进入、场景离开、选项显示、选项选择四个明确时间点执行；RuntimePlugin 仍是变量唯一写入口。
- Blockly 是 RuleDocument 的编辑投影；积木变更生成创作命令，重新打开时从规则事实生成积木。
- 时间和生存作为 `rulePack` 贡献声明变量、函数、积木、说明与运行处理；卸载时保留作品数据并产生明确缺失贡献问题。

### 6.6 类型化插件合同

- 收敛为有当前消费者的贡献：`runtime`、`rulePack`、`validator`、`analyzer`、`layout`、`editorTheme`、`playerTemplate`、`contentRenderer` 和 `editorTool`。
- manifest 明确 id、版本、兼容范围、依赖、冲突、默认状态、设置 schema 和贡献能力。
- PluginSystem 继续作为注册状态、生命周期、Event、Data、贡献、配置和健康状态的唯一 owner。
- 每个注册项记录 owner；禁用、卸载和激活失败统一清理 owner 资源。配置变更先验证并可回滚。
- 插件间通知只通过 Event API；规则、模板和布局使用类型化贡献，不使用宽泛生命周期数据管道。

### 6.7 播放器模板 SDK

- `PlayerTemplateManifest` 声明 id、名称、版本、分类、预览、能力、设置 schema、默认值、场景变体、资源和降级策略。
- `PlayerTemplate` 提供 shell、样式、渲染绑定和可选脚本，只通过 Player API 工作。
- `TemplateRegistry` 负责内建与本地模板的统一注册、冲突检测、查询和懒加载缓存；核心不得识别具体模板 ID。
- `TemplateCompiler` 接收 StoryDocument、模板、实际使用的规则贡献和资源，生成网页试玩或完全内联的独立 HTML。
- 模板设置与单场景变体进入 StoryDocument presentation，通过统一创作命令校验、保存、撤销、导出和恢复。
- 不支持某项表现时按 manifest 公开降级：例如人物层转为带名称插图、热区转为可访问的地点按钮；不得静默删除。

### 6.8 十二套内建模板合同

现有两套必须先做等价迁移，再新增十套。名称可在不改变结构身份的前提下做中文润色，但不得把结构差异退化为换色。

| 序号 | 模板 | 结构身份 | 必须保留或证明的差异 |
| --- | --- | --- | --- |
| 01 | 沉浸视觉小说 | 当前全屏背景、人物层、底部对白 | 与当前视觉小说逐状态等价 |
| 02 | 即时聊天 | 当前手机容器、消息流、聊天菜单 | 与当前聊天播放器逐状态等价 |
| 03 | 章节书页 | 双页/单页阅读、章节页眉、页下注记 | 选择随阅读流组织，不使用视觉小说底栏 |
| 04 | 极简文字冒险 | 聚焦正文与命令式选择历史 | 空间资源转为正文内可访问入口 |
| 05 | 舞台剧场 | 舞台、角色站位、剧本式台词区 | 人物与旁白采用舞台调度结构 |
| 06 | 调查档案 | 案卷栏、证据区、记录正文 | 地图、属性、背包组织为调查工具 |
| 07 | 旅行手账 | 手账页、地点索引、插图批注 | 访问记录与空间探索成为主导航 |
| 08 | 空间图鉴 | 主场景画面、地点图例、热点清单 | 热区与图片探索优先且具键盘替代入口 |
| 09 | 分镜漫画 | 分镜格、对白框、选项转场 | 长文本和多人物有明确分页降级 |
| 10 | 广播剧本 | 场次列表、对白轨、旁白轨 | 无图片时仍保持完整叙事层次 |
| 11 | 记忆相册 | 相片舞台、说明卡、记忆索引 | 图片、人物与访问历史形成相册结构 |
| 12 | 桌面卷宗 | 多窗格正文、状态侧栏、任务菜单 | 桌面玩家高密度信息但不嵌套卡片堆叠 |

每套模板必须具有独立 shell 结构、非单色依赖的完整视觉系统、设置项、至少两个场景变体、预览图、长文本策略、能力声明、键盘路径、手机玩家降级和统一测试夹具。不得复制参考仓库的视觉资产或实现。

### 6.9 存储、资源和当前文件格式

- IndexedDB 提升 schema 版本并断裂式清除旧结构，只存当前 StoryDocument、StoryEditorState、revision、资源、设置和插件配置。
- 当前启动事务种入两份覆盖核心能力的示例作品；初始化标记防止刷新重复创建。
- 保存继续使用串行协调器、expected revision、Web Locks 和 BroadcastChannel；提交快照而不是 React 闭包中的旧对象。
- 资源继续按内容哈希分离存储，统一管理 object URL，删除作品和导入失败后正确回收孤立资源。
- 数据备份只保留整库 ZIP，包含全部作品、编辑投影、revision、资源、设置和插件配置；单作品独立 HTML 继续作为发布产物，当前 manifest 与 schema 只接受当前版本。
- 导入先在内存中完整解码、schema 校验、引用校验、哈希和容量检查，再在一个事务中提交。

### 6.10 UI 接线原则

- 保留现有路由、页面组件的用户职责和 CSS 结果；允许按变化原因拆分内部文件，但外层 DOM landmark、class 语义和交互不随意改变。
- Dashboard、Editor 和 PluginStore 不直接访问 IndexedDB、文件解码器或插件内部实例，改由应用服务与类型化端口提供能力。
- React Flow 只在 Editor 内通过 adapter 消费投影；节点与连线交互生成命令，不直接修改领域数组。
- 现有侧栏、底部编辑区、图片热区编辑、变量管理和 Blockly 面板保持位置与使用方式；只替换其数据入口。
- 模板选择在当前播放器设置附近增量呈现：先保留当前选择控件，再提供与当前视觉一致的图库弹层和真实预览，不改作品库主信息架构。
- 图标优先使用现有图标体系；没有既有依赖时才评估新增图标库，不能因图标迁移改变整体视觉。

### 6.11 预览、导出和隔离

- 网页试玩和独立 HTML 由同一 TemplateCompiler 产物驱动，不维护第二份播放器业务源码。
- 编辑器预览在受控 iframe 中运行模板，模板通过消息端口接收快照和提交命令，不访问编辑器 DOM 或 IndexedDB。
- 独立 HTML 内联所选模板、当前作品、引用资源与实际规则贡献，不包含未使用模板和编辑器代码。
- 本地模板/插件包安装前展示来源和脚本影响，校验路径、大小、哈希、manifest 与设置；失败原子回滚。
- 静态 catalog 只做本地清单适配，不提供联网安装入口。

### 6.12 性能、职责和可访问性

- 保持路由懒加载；首页不初始化插件运行时，不加载 React Flow、Blockly、模板脚本或播放器代码。
- Editor 内按功能懒加载 React Flow 与 Blockly；模板图库首屏只加载 manifest 和小预览，选中后才加载实现。
- 图分析继续使用 SCC 缩合 DAG；大图仅渲染可见节点；资源 Blob 不进入 StoryDocument。
- T001 基线预算：`verify <= 15s`，单次生产构建命令 `<= 5s`；入口脚本 `<= 210kB` 且 gzip `<= 70kB`，Editor `<= 250kB`，Blockly `<= 900kB`；每个模板按需加载且单模板脚本/CSS合计 `<= 180kB`，构建总量 `<= 16MiB`；不含作品图片的独立 HTML `<= 1.5MiB`；原 7 条 E2E 链路 `<= 25s`，扩展全套浏览器验收 `<= 180s`。
- 压力预算：100/500/1000 节点编辑器加载分别 `<= 300/600/900ms`，刷新 `<= 250/350/500ms`，记录堆分别 `<= 64/128/160MiB`；新增搜索、移动、连接、正文、模板切换和试玩步骤单项不得依赖放宽 Playwright 全局超时通过。
- 超过 300 行的 TypeScript/TSX 文件逐个做职责审查，只在存在多个变化原因时拆分；CSS 按视觉所有权组织，不机械切碎或重写。
- 保持语义化标签、键盘、焦点、对比度、减少动画、浏览器缩放、长文本和手机玩家可用性。

### 6.13 文档边界

- `spec.md` 仅在能力通过验收后更新为最终当前事实，不提前写入模板平台或新格式。
- README 保留现有品牌定位、在线地址、主要能力和本地启动，不写内部架构报告。
- SECURITY 说明本地数据、导入包、本地脚本模板、预览隔离和独立导出边界，不夸大安全承诺。
- CONTRIBUTING 说明当前 schema、模板/插件包合同、视觉等价要求和验证命令。
- AGENTS 与 skill 只有在真实规则或命令变化时同步；不创建额外规则体系。

### 6.14 依赖安全收口

- 将 `react-router-dom` 精确锁定为 `6.30.4`，禁止使用会漂移到未经审计版本的范围依赖；由其依赖合同同步锁定 `react-router@6.30.4` 及安全的 `@remix-run/router`。
- 使用 `npm.cmd install --save-exact react-router-dom@6.30.4` 通过 npm 维护 manifest、锁文件和安装树，不手工拼改 lockfile。
- 先验证完整与生产审计归零及 `npm.cmd ls` 解析，再运行 type-check、路由相关 E2E、verify、test:e2e、test:stress、verify:full 和冻结视觉基线。
- `verify:full` 在同一命令内先以独立 Playwright 进程运行 stress，再运行 E2E，使性能测量不受前一套重负载验收污染；仍覆盖全部 19 项并保持 `--max-failures=1 --retries=0`，不得改预算、断言或重试次数。

## 7. 实施任务

- [x] **T001 Research、命令基线与仓库保护**：完整读取本计划涉及的 UI、CSS、Domain、Engine、Player、Plugin、Blockly、存储、导出、PWA、测试与配置；逐个研究九个 `reference/` 仓库当前提交中与故事格式、编辑器/运行时分离、模板合同和扩展生命周期直接相关的实现，记录可借鉴原则与不可复制内容；确认 `reference/` 保护边界；运行当前 verify、build、E2E、stress 并记录耗时、用例、产物、chunk、控制台和内存基线。
- [x] **T002 视觉与交互冻结基线**：使用固定种子数据捕获首页、声明、作品库、编辑器全部主要面板、插件商店、视觉小说与聊天播放器的开始/游玩/菜单/结局状态；覆盖 1440×900、1920×1080 和手机玩家视口；固化文案、landmark、控件顺序、关键样式、归一化边界框、焦点和操作步数；确认 `09d8d2f` 全部通过后再允许改 UI 文件。
- [x] **T003 失败测试与静态边界**：为 F001–F018 建立单元、集成、静态、E2E、视觉、模板合同、离线、恢复和压力测试骨架；证明目标缺口在旧实现上按预期失败，现有能力与体验保护测试保持通过；禁止具体模板 ID、Domain 反向依赖和并行核心。
- [x] **T004 当前 StoryDocument 与编辑投影**：建立独立 StoryDocument、StoryEditorState、Scene、Content、Choice、Media、Variable、Rule、Presentation 和 ExtensionData；Choice 保存唯一 targetSceneId；实现 React Flow adapter；迁移验证、分析和布局；删除旧 React Flow 领域别名与 Edge 事实，不留双写或转发。
- [x] **T005 统一创作 API 与事务**：实现版本化 schema、查询、dry-run、原子批量命令、expected revision、结构化 diff、撤销和重做；覆盖所有故事与编辑投影修改；把现有 UI handler 逐个接入并在每个面板后运行视觉/交互等价测试。
- [x] **T006 唯一 PlayerKernel**：从 CoreEngine 与 PlayerCore 提炼模板无关的 PlayerKernel、PlayerSnapshot、PlayerCommand、规则时间点、历史、访问记录和存档合同；拆出保存适配器、菜单投影和内容渲染；先用当前播放器测试证明行为等价，再删除重复业务实现。
- [x] **T007 规则与变量事实统一**：实现 RuleDocument、RuleEngine、条件/动作注册表和 RuntimePlugin 写入口；把节点进入/离开、选项显示/选择迁入；让 Blockly 从规则事实投影并通过命令回写；迁移时间、生存贡献与测试。
- [x] **T008 插件合同收敛**：迁移现有验证、分析、布局、主题、Markdown、内嵌选项、Blockly、时间和生存能力到类型化贡献；完成 owner 清理、依赖、冲突、健康、设置校验与回滚；删除宽泛 `any` Hook 和无消费者接口，同时保持插件商店视觉与操作不变。
- [x] **T009 存储、资源、保存与当前文件 schema**：升级 IndexedDB 并断裂式使用当前结构；适配 revision、锁、广播、资源哈希、回收和保存协调；只保留完整工作区 ZIP 备份恢复与单作品独立 HTML；完成损坏包、竞态、刷新、关闭和多标签测试；删除单作品 JSON/ZIP、旧解析与兼容入口。
- [x] **T010 模板 SDK、注册表与本地包**：实现 manifest、能力、设置、场景变体、资源、降级、Player API、TemplateRegistry 和加载缓存；内建与本地模板走同一合同；完成安装、卸载、持久化、失败回滚和静态 catalog 适配。
- [x] **T011 TemplateCompiler 与统一预览/导出**：建立同源网页试玩、iframe 预览和独立 HTML 编译链；内联实际模板、故事、资源和规则贡献；移除固定视觉小说构建模板与导出特判；保持现有 HTML 导出按钮位置和用户反馈；新增独立产物零网络请求验证。
- [x] **T012 视觉小说播放器等价迁移**：把当前 VisualNovelPlayer、GameMenu 和专用样式迁为模板 01；逐状态对比 DOM、文案、计算样式、截图和交互；所有现有能力等价后才删除旧页面业务接线，保留必要视觉实现而非重写。
- [x] **T013 聊天播放器等价迁移**：把当前 ChatStylePlayer、ChatGameMenu 和专用样式迁为模板 02；逐状态对比手机容器、消息流、选择、菜单、存档、地图、属性、背包和结局；等价后删除旧路由特判，不改变当前视觉。
- [x] **T014 模板 03–05**：完成章节书页、极简文字冒险和舞台剧场的 shell、完整视觉系统、设置、场景变体、降级、预览和统一行为测试；逐套使用规范作品完成桌面、手机、file:// 与零请求验收并立即记录证据。
- [x] **T015 模板 06–08**：完成调查档案、旅行手账和空间图鉴的全部合同、视觉和统一行为矩阵；重点验收地图、属性、背包、访问记录、图片热区和键盘替代入口。
- [x] **T016 模板 09–12**：完成分镜漫画、广播剧本、记忆相册和桌面卷宗的全部合同、视觉和统一行为矩阵；重点验收长文本、多人物、无图片、手机降级和高密度状态布局；确认十二套结构指纹不重复。
- [x] **T017 现有 UI 增量接线**：在不重排 Dashboard、Editor 和 PluginStore 的前提下接入模板选择、真实预览、能力提示、设置和场景变体；保持原路由、文案、控件层级和操作路径；所有编辑写入通过 Authoring API。
- [x] **T018 本地创作端口与扩展错误回路**：实现只读查询/schema/submit 浏览器端口、变更审阅、全部/部分接受、拒绝、过期 revision、无效引用和撤销；实现本地模板/插件包错误呈现与失败回滚；任何失败不改变作品且不阻断离线编辑。
- [x] **T019 示例作品与完整功能夹具**：用自有测试内容建立覆盖分支、循环、多结局、Markdown、内嵌选择、人物、图片、热区、变量、四类规则时间点、时间、生存、三个存档槽和模板切换的规范作品；保留现有两份首次示例的用户价值和视觉，不复制参考资产。
- [x] **T020 性能、可访问性与职责收口**：按 T001 基线优化路由、React Flow、Blockly、模板和资源加载；完成 100/500/1000 节点全链压力；审查超过 300 行文件并按变化原因拆分；通过键盘、焦点、对比度、减少动画、缩放、长文本、桌面两视口和手机玩家验收。
- [x] **T021 测试、恢复演练、文档与清理**：补齐 F001–F018 长期测试；演练损坏导入、扩展失败、保存竞态、整库恢复、离线刷新和 file://；同步 README、spec、SECURITY、CONTRIBUTING 与必要规则；删除旧核心、旧播放器接线、旧 schema、旧测试、固定模板路径和无消费者文件，确认现有体验仍全绿。
- [x] **T022 最终生产验收与收口**：依次运行第 8 节所有命令和人工矩阵；对十二模板逐套生成三视口截图与独立 HTML；确认现有视觉/交互基线无回归、全部保留能力可发现、F001–F018 与 G001–G010 满足、未完成项归零；填写第 9 节真实结果。未经授权不 commit、不 push、不部署。
- [x] **T023 生产依赖安全收口**：精确锁定 react-router-dom/react-router 到审计给出的 6.30.4 安全版本；确认完整与生产审计均无 high，不引入无关依赖变化；完成路由、日常、浏览器、压力、全量、视觉与本地入口验收，更新第 9 节真实结果。

## 8. 验证计划

### 8.1 执行中局部验证

- Domain、AuthoringSession、PlayerKernel、RuleEngine、RuntimePlugin、PluginSystem、TemplateRegistry、TemplateCompiler、WorkspaceRepository、保存协调器、归档和创作端口分别运行无 DOM 单元测试。
- 每迁移一个现有 UI 面板，立即运行该面板交互测试、当前视口截图对比和保存刷新测试，失败时不得继续批量删除旧实现。
- 每完成一套模板，立即运行统一模板合同、规范作品浏览器用例、file:// 零请求和三视口截图，不在十二套结束后一次性补测。
- 测试使用确定性 ID、时间、随机数和本地自有素材；不得依赖 `reference/`、外网或私人文件。

### 8.2 日常门禁

```powershell
npm.cmd run verify
```

- 只包含类型检查、确定性单元测试、生产构建和静态边界。
- 不隐式加入 Playwright、截图或压力测试。
- 实际耗时、测试数量、构建体积和与 T001 的差异写入第 9 节。

### 8.3 浏览器验收

```powershell
npm.cmd run test:e2e
```

至少覆盖：

- `/mo/`、Hash 深链、首页、声明、背景图、微信图片、二维码、作品库和离线刷新。
- 创建、打开、删除、编辑、所有主要面板、自动保存、关闭提示、第二标签只读和 revision 冲突。
- 节点、连接、搜索、布局、验证、分析、图片、人物、热区、变量、Blockly、时间、生存和指定节点预览。
- 整库 ZIP 导出覆盖恢复、独立 HTML、损坏包和原子失败。
- 插件搜索、分类、启停、依赖、冲突、主题、健康、配置和本地包失败回滚。
- 两套现有播放器逐状态等价，以及十二模板选择、设置、场景变体、切换、存档、菜单、地图、属性、背包、结局和重开。
- 创作命令 dry-run、无效批次、过期 revision、审阅拒绝、部分接受、全部接受和撤销。

### 8.4 视觉等价验收

- 在相同 Chromium 版本、种子数据和禁用非确定动画的条件下，对 T002 与最终状态逐图比较。
- 冻结页面覆盖 1440×900 和 1920×1080；聊天播放器及全部导出玩家增加 390×844。
- 自动断言页面无水平溢出、关键控件可见、文本不遮挡、弹层不越界、焦点不丢失和 console error 为零。
- 人工逐图确认非空、层级、配色、排版、间距、专用视觉和现有气质保持；新增模板单独验收，不拿新增区域替换旧基线。
- 视觉失败只能修实现，不能更新基线或提高容差来收口。

### 8.5 十二模板与独立产物矩阵

- 每套模板使用同一规范作品生成网页预览和独立 HTML。
- 依次执行开始、正文、Markdown、内嵌选择、普通选择、条件选择、图片、人物、热区、变量 HUD、菜单、三个存档槽、读档、地图、属性、背包、结局和重开。
- 切换模板前后比较最终场景、变量、历史、访问记录和存档 schema，必须一致。
- 从 `file://` 打开独立 HTML，捕获并断言 HTTP(S) 请求为零、console error 为零、资源缺失为零。
- 检查未含作品图片的单文件体积与 T001 基线预算；只包含实际使用模板和运行贡献。

### 8.6 常规压力

```powershell
npm.cmd run test:stress
```

- 100、500、1000 节点分别覆盖打开编辑器、搜索、选择、移动、连接、正文编辑、保存、刷新恢复、模板切换和试玩。
- 记录加载、关键操作、刷新、长任务、可见节点数和记录堆；预算由 T001 当前机器实测制定并写回 6.12。
- 不修改 Playwright 全局、用例或断言超时来换取通过，不把压力测试加入日常门禁或 Pages workflow。

### 8.7 全量验收

```powershell
npm.cmd run verify:full
```

- `verify:full` 仅用于最终人工全量验收，必须覆盖 E2E 与 stress 当前目录。
- 任何失败都回到对应实现和局部测试修复；不得靠重试、跳过、删断言或放宽超时通过。

### 8.8 本地生产入口

```powershell
npm.cmd run start
```

- 验证 Python 入口完成生产构建并从 `/mo/` 打开。
- 验证 Hash 深链、Service Worker、离线重载、模板懒加载和资源路径。
- 保持服务运行到确认入口可访问后再正常停止；未获授权不触发外部部署。

### 8.9 静态清理与文档核验

- `rg` 确认 Domain 不依赖 React Flow/UI/browser，核心不存在具体模板 ID 分支，UI 不直接访问 IndexedDB 或插件内部实例。
- `rg` 确认不存在旧 Story/Edge 双写、旧 schema、固定播放器构建路径、兼容转发、废弃插件 API 和无消费者旧文件。
- 核对 README、spec、SECURITY、CONTRIBUTING 与真实代码、命令和页面一致；首页与声明文案仍来自 `src/content/`。
- 核对 `reference/` 内容和九个嵌套仓库状态未被修改；主仓库状态只包含当前任务文件和 owner 已有改动。

### 8.10 生产依赖安全验收

```powershell
npm.cmd audit --omit=dev --audit-level=high
npm.cmd audit --audit-level=high
npm.cmd ls react-router-dom react-router --all
npm.cmd run type-check
npm.cmd run test:e2e
npm.cmd run test:stress
npm.cmd run verify:full
npx.cmd playwright test tests/visual/experience-baseline.spec.ts --project=chromium --max-failures=1 --retries=0
npm.cmd run start
```

- 两次 audit 必须均报告 0 个 high/critical，安装树必须只有 `react-router-dom@6.30.4 -> react-router@6.30.4` 及其安全的 `@remix-run/router`；若仍有 moderate，必须记录其公告与当前纯前端攻击面，不得伪称全量 audit 为零。
- `start` 继续验证 `/mo/`、Hash 深链、Service Worker、离线刷新和模板单 iframe 懒加载，验收后停止服务。

## 9. 收口

### 9.1 完成条件

- T001–T023 全部逐项完成并即时勾选。
- F001–F019 全部有长期验证且通过。
- G001–G011 全部有代码、入口、测试、截图和文档事实支撑。
- 当前首页、声明、作品库、编辑器、插件商店、视觉小说和聊天播放器无视觉或交互回归。
- 十二模板全部通过统一功能、结构差异、视觉、设置、降级、手机、离线和独立导出矩阵。
- 所有现有能力在原信息架构中仍可发现并通过主链路验收。
- `verify`、`test:e2e`、`test:stress`、`verify:full` 和 `start` 均有本次真实运行结果。
- 完整与生产依赖审计均为 0 个 high/critical，路由安装树只解析 `6.30.4` 安全组合；2 个 moderate 上游公告已记录为剩余风险。
- 不存在兼容层、双核心、双写、固定模板特判、临时文件、跳过测试、伪造结果或“后续处理”占位。

### 9.2 执行完成后填写

- 完成事实：T001–T023 全部完成；F001–F019 的长期证据映射由 AcceptanceMatrix 单测及依赖审计命令验证存在并随最终单元、浏览器、视觉、压力和静态门禁实际通过；G001–G011 均有当前实现、入口、测试、截图和文档支撑。
- 体验等价结果：`tests/visual/experience-baseline.spec.ts` 最终 1 项通过；6.30.4 通过官方 `future.v7_startTransition` 恢复 7.x 的懒路由 transition 行为后，冻结页面与两套现有播放器保持 `09d8d2f` 基线；chat-ending 只使用既定最多 16 个、单通道差值不超过 1 的 Chromium 合成像素容差，未更新基线或放宽断言。
- 十二模板结果与截图位置：模板功能、三视口网页矩阵和十二套 `file://` 独立 HTML 均通过；HTTP(S) 请求和 console error 为零，菜单与三个存档槽可用；网页 36 张、独立 HTML 36 张，共 72 张，位于 `artifacts/experience-current/09d8d2f/templates/`。
- `npm.cmd run verify`：首次运行由双结局规范夹具揭示 ValidatorPlugin 测试前提过时；修正测试前提后从头重跑，用时约 `5.7s`，19 文件 65 项单测、类型、构建和静态边界全部通过。
- `npm.cmd run test:e2e`：用时约 `44.8s`，16 项 Chromium 用例全部通过。
- `npm.cmd run test:stress`：用时约 `26.2s`，100/500/1000 节点 3 项全部通过；该次加载 `118/216/329ms`、刷新 `89/332/446ms`，最大操作 `1292ms`，1000 节点 19 个长任务、最大 `90ms`。
- `npm.cmd run verify:full`：依赖及路由修复后从头运行用时约 `106.2s`，19 文件 65 项单测及 19 项 Chromium E2E/压力全部通过；最终压力加载 `119/214/340ms`、刷新 `92/291/462ms`，最大操作 `1302ms`，1000 节点 20 个长任务、最大 `86ms`。
- `npm.cmd run start`：完成构建并从 `http://127.0.0.1:4567/mo/` 提供服务；`/mo/` 返回 200，Hash 深链、Service Worker 接管和离线刷新通过；模板图库只有 1 个选中模板 iframe，11 个未选模板均未加载 iframe，真实视觉小说模板 `srcdoc` 为 17092 字节，console error 为零；验收后服务与本次子进程已停止，4567 监听数为零。
- 生产依赖审计：`npm.cmd audit --omit=dev --audit-level=high` 与 `npm.cmd audit --audit-level=high` 均退出 0，high/critical 为 0；安装树唯一解析为 `react-router-dom@6.30.4 -> react-router@6.30.4 -> @remix-run/router@1.23.3`，未运行 `npm audit fix --force`。
- 构建与性能数据：405 个模块；入口 `159.06kB`（gzip `52.93kB`）、Editor `239.02kB`（gzip `72.52kB`）、Blockly `851.86kB`（gzip `228.71kB`）；PWA 预缓存 46 项、`2108.84KiB`，均在 T001 预算内。
- 恢复与离线演练：整库往返以及坏 ZIP、错误 manifest、损坏作品、缺图、错误 MIME、哈希不符、重复资源、容量不足和事务提交失败均验证失败前后工作区不变；浏览器整库覆盖恢复、坏包失败、本地模板失败回滚、首次加载后离线刷新和十二套独立 HTML 均通过。
- 未验证内容：无任务要求内未验证项；外部部署未获授权，不属于本次执行项。
- 剩余风险：完整 audit 仍报告 React Router 的 2 个 moderate：反斜杠开放重定向 `GHSA-wrjc-x8rr-h8h6` 与 SSR hydration 构造器注入 `GHSA-337j-9hxr-rhxg`；墨水不接收外部跳转目标且为 HashRouter 纯前端应用，不使用 SSR/hydration。npm 的唯一自动建议会回到含 2 个 high 的 7.18.2，因此不运行 `npm audit fix --force`；当前类型、测试、构建、边界、浏览器、压力、视觉与本地生产入口均通过。
- `reference/` 状态：Chapbook `2385f90`、Harlowe `16e121d`、Ink `35c63e5`、InkJS `fcf4602`、Inky `3d46aa9`、Snowman `1c9d990`、SugarCube `ba17b12`、Twine Specs `74b3d89`、TwineJS `e36c600`，九个工作树均干净；`reference/` 已作为本地研究目录进入根 `.gitignore` 且未修改。
- commit / push / deploy：架构收口提交 `cac7ca1` 已按 owner 授权推送；T023 尚未获得新的 commit/push 授权，未部署。

## 执行规则

- 新窗口先完整读取 `AGENTS.md`、`spec.md`、本文件和两份仓库 skill，再从当前未完成任务继续执行。
- Checklist 随真实进度即时更新，不在结束时一次性补勾。
- 新事实推翻需求、事实、失败测试、设计或预算时，先更新本文件，再继续实现。
- UI 修改前必须完成 T002；现有视觉或交互等价测试失败时，不得以架构完成为由继续删除旧实现。
- 严格保护 `reference/` 和 owner 改动；清理命令不得覆盖本地研究仓库、用户数据、测试下载或任务范围外文件。
- 不重复询问本计划已经明确的产品与工程决策；不因工作量大停在分析、建议、阶段总结或半成品。
- 遇到客观阻塞先穷尽仓库内可行方案，再记录命令、事实、已完成内容与剩余风险。
- 未经 owner 当前明确授权，不 commit、不 push、不触发外部部署。
